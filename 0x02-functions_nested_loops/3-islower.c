#include "main.h"
/**
 * _islower - checks if letter is lower or uppercase
 * @c: takes in character
 * Return: 0 after code completion
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
